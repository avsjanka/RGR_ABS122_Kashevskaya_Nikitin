#pragma once
#include <random>
#include <algorithm>
#include <vector>
#include <cmath>
#include <fstream>
using namespace std;
vector<char> notebook_crypt(vector<char>& letter, vector<vector <char>>& key);