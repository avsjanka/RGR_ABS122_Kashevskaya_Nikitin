#pragma once
#include <random>
#include <algorithm>
#include <vector>
#include <cmath>
#include <fstream>
using namespace std;
vector<char> notebook_encrypt(vector<char>& crypt_letter, vector<vector <char>>& key);